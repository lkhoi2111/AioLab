[phases.setup]
nixPkgs = [
  "nodejs_22",
  "python311",
  "ffmpeg",
  "yt-dlp"
]

[phases.install]
cmds = [
  "npm install",
  "python -m pip install --upgrade pip",
  "pip install librosa soundfile numpy scipy demucs"
]

[start]
cmd = "npm start"
